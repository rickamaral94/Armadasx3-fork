# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
#
# 6g, raised from 4098m. assembleGithubRelease died with "Java heap space" after
# 6m48s on a 16 GB runner. This app is a heavy client for the daemon: Compose
# compilation, R8 over the whole Kotlin/Java surface, and packaging a ~94 MB
# native core in one process. 4 GB left no headroom for R8's global analysis.
#
# 6g rather than more, because this is the committed default every contributor
# inherits: it fits a 16 GB machine alongside an IDE, which is what building
# this project already requires. CI raises it further through GRADLE_OPTS.
org.gradle.jvmargs=-Xmx6g -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# org.gradle.parallel=true
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
# R8 full mode off.
#
# Full mode's extra optimisation passes emitted a synthetic lambda that ART then
# refuses to verify:
#   VerifyError: Rejecting invocation, expected 5 argument registers,
#   method signature has 6 or more
# It aborts in androidx.startup before any of our code runs, so the release build
# never launches. A -keep rule cannot fix it: the offending classes
# (Foo$$ExternalSyntheticLambdaNN) are generated BY R8, so they do not exist when
# keep rules are evaluated.
#
# R8 stays on -- shrinking, obfuscation and normal optimisation all still run, so
# the APK stays ~57 MB rather than ballooning back to a debug-sized build.
android.enableR8.fullMode=false
